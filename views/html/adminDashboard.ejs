<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<style>
    .img{
        border-radius: 50%;

    }
</style>
<body>
    <%- include('adminNavbar') %>


    <H1>ADMIN Dashboard</H1>
  

    <form action="">
        <input type="text" placeholder="Search Your Email and Mobile Number" name="search" id="">
        <input type="submit"  value="Search">
    </form>


<table>
    <tr>
        <th>Name</th>
        <th>Email</th>
        <th>Image</th>
        <th>Verified</th>
        <th>Action</th>
    </tr>

    <%
    if(users.length > 0 ){
        for(let i=0 ; i<users.length ; i++){
            %>
            <tr>
                <td><%= users[i].name%></td>
                <td><%= users[i].email%></td>
                <td>
                <img class="img" src="/userImages/<%= users[i].image%>" alt="<%= users[i].image%>" height="50px" width="50px" ></td>
                <td>
                    <% 
                    if(users[i].is_verified == 0 ){
                        %>
                        <p>Not Verified</p>
                        <%
                    }else{
                        %>
                        <p>Verified</p>
                        <%
                    }   
                    %>
                </td>
                <td>
                    <a href="/admin/edit-user?id=<%=users[i]._id%>">Edit</a><br>
                    <a href="/admin/delete-user?id=<%=users[i]._id%>">Delete</a><br><br>
                    
                </td>
            </tr>
            <%
        }

    }else{
        %>
        <tr>
            <td>User Not Found</td>
        </tr>
        <%
    }
    %>



</table>

<br>
<%

for(let j=1 ; j<=totalPages ; j++){
%>
<a href="?page=<%= j %>"><%= j %></a>
<%

}
%>
<a href="?page=<%= previous %>">Previous</a>
<a href="?page=<%= next %>">Next</a> 
<br>
<br>

</body>
</html>